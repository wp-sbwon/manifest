# Runtime dependencies; kept in sync with pyproject.toml [project.dependencies].
# Dev/test: pip install -e ".[dev]"
# OpenCode (optional): pip install -e ".[opencode]" or see pyproject.toml [project.optional-dependencies]
textual>=0.40.0
GitPython>=3.1.40
watchdog>=3.0.0
