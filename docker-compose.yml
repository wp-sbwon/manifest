version: '3.8'

services:
  app:
    build: .
    container_name: manifest-app
    volumes:
      - .:/app
    stdin_open: true
    tty: true
    environment:
      - PYTHONUNBUFFERED=1
      - PYTHONPATH=/app/src
    command: python -m manifest