# Test fixtures

## Calculator

**Mock project:** `calculator/`

Used by the bottom-up pipeline integration test: CodeExtractor runs on this tree; merge with design produces a valid blueprint. See `tests/integration/test_bottomup_pipeline.py`.
