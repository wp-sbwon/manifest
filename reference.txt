import React, { useState, useEffect } from 'react';

const App = () => {
  // State for navigation and selection
  const [mainTab, setMainTab] = useState('DIAGRAM'); // DIAGRAM, FILES, TIMELINE, MISSION
  const [infoView, setInfoView] = useState('DESIGN'); // DESIGN, DIFFERENCES
  const [selectedId, setSelectedId] = useState('PROJECT_ROOT');
  const [currentTime, setCurrentTime] = useState(new Date().toLocaleTimeString());

  // Update clock
  useEffect(() => {
    const timer = setInterval(() => setCurrentTime(new Date().toLocaleTimeString()), 1000);
    return () => clearInterval(timer);
  }, []);

  // Theme configuration
  const colors = {
    bg: '#0c0c0c',
    panel: '#111111',
    border: '#333333',
    text: '#cccccc',
    dim: '#666666',
    blue: '#569cd6',   // Module / Root
    cyan: '#4fc1ff',   // Method
    magenta: '#c586c0', // Gateway (I/O)
    green: '#4ec9b0',   // Healthy / Done
    yellow: '#dcdcaa',  // Partial / Active
    red: '#f44747',     // Deviation / Alert
    gray: '#444444',    // Planned / Pending
  };

  const S = "■"; // Status indicator block

  // Project Data Structure
  const projectData = {
    'PROJECT_ROOT': {
      name: 'System Core',
      type: 'Root',
      description: 'A high-performance CLI calculator engine built with zero external runtime dependencies.',
      status: 'green',
      planning: {
        interface: 'Binary Execution -> Console Output',
        rules: ['No external network access allowed.', 'Binary size under 5MB.', 'Modular communication only.'],
        style: 'Monolithic Core'
      },
      actual: { interface: 'v1.2.4-stable', deps: ['std_lib'], sideEffects: 'None', complexity: 'Low' },
      isDeviating: false
    },
    'CLI_PARSER': {
      name: 'CLI_PARSER',
      type: 'Gateway',
      description: 'Translates raw terminal input into structured command objects.',
      status: 'green',
      planning: {
        interface: 'raw_string -> command_object',
        rules: ['Sanitize all inputs.', 'Handle empty arguments gracefully.'],
        style: 'Stream Processor'
      },
      actual: { interface: 'string -> Object', deps: ['argparse'], sideEffects: 'None', complexity: 'Medium' },
      isDeviating: false
    },
    'MUL_METHOD': {
      name: 'MUL_METHOD',
      type: 'Method',
      description: 'Arithmetic worker for multiplication using bit-level optimization.',
      status: 'red',
      planning: {
        interface: 'a: num, b: num -> product: num',
        rules: ['Must be side-effect free.', 'Must use bit-shifting for speed.'],
        style: 'Bitwise Worker'
      },
      actual: {
        interface: 'a: any, b: any -> any',
        deps: ['logger.py', 'fs'],
        sideEffects: 'File Write (logs)',
        complexity: 'O(n) - Iterative'
      },
      isDeviating: true
    },
    'ADD_METHOD': {
      name: 'ADD_METHOD',
      type: 'Method',
      description: 'Standard addition worker with overflow protection.',
      status: 'green',
      planning: {
        interface: 'a: num, b: num -> sum: num',
        rules: ['Commutative property must hold.', 'Handle Infinity overflows.'],
        style: 'Pure Function'
      },
      actual: { interface: 'a: num, b: num -> num', deps: ['math'], sideEffects: 'None', complexity: 'Low' },
      isDeviating: false
    }
  };

  const selected = projectData[selectedId] || projectData['PROJECT_ROOT'];

  // Helper: ANSI-style rendering simulation
  const ColorSpan = ({ color, children, className = "" }) => (
    <span style={{ color }} className={className}>{children}</span>
  );

  return (
    <div className="flex flex-col h-screen bg-black text-[#ccc] font-mono p-4 text-[11px] leading-tight select-none overflow-hidden">

      {/* HEADER */}
      <div className="flex justify-between items-center border-b border-[#333] pb-2 mb-2 px-2 bg-[#111] h-8 shrink-0">
        <div className="flex items-center gap-4">
          <ColorSpan color={colors.blue} className="font-bold border-r border-[#333] pr-4">VIBE_DASHBOARD v1.6</ColorSpan>
          <div className="flex gap-4 uppercase tracking-widest text-[9px]">
            <span className={infoView === 'DESIGN' ? 'text-white font-bold' : 'text-[#555]'}>[ Planning ]</span>
            <span className={infoView === 'DIFFERENCES' ? 'text-red-500 font-bold underline' : 'text-[#555]'}>[ Differences ]</span>
          </div>
        </div>
        <div className="flex gap-6 text-[9px] text-[#555]">
          <span>STATUS: <ColorSpan color={selected.isDeviating ? colors.red : colors.green} className="font-bold">{selected.isDeviating ? 'DEVIATION DETECTED' : 'HEALTHY'}</ColorSpan></span>
          <span>TIME: {currentTime}</span>
        </div>
      </div>

      <div className="flex flex-1 gap-2 overflow-hidden">

        {/* LEFT SIDEBAR: HEALTH & TASKS */}
        <div className="w-52 flex flex-col gap-2 shrink-0">
          <div className="h-32 border border-[#333] bg-[#0c0c0c] p-2 flex flex-col">
            <div className="text-[9px] text-[#555] font-bold uppercase border-b border-[#222] pb-1 mb-2">Project Health</div>
            <div className="space-y-1.5 text-[9px]">
              <div className="flex justify-between"><span>Total Deviation:</span><ColorSpan color={colors.yellow} className="font-bold">12%</ColorSpan></div>
              <div className="flex justify-between"><span>Code Quality:</span><ColorSpan color={colors.green}>Excellent</ColorSpan></div>
              <div className="flex justify-between"><span>Test Coverage:</span><ColorSpan color={colors.blue}>88%</ColorSpan></div>
              <div className="flex justify-between"><span>Binary Size:</span><ColorSpan color={colors.text}>1.2 MB</ColorSpan></div>
            </div>
          </div>

          <div className="flex-1 border border-[#333] bg-[#0c0c0c] p-2 flex flex-col overflow-hidden">
            <div className="text-[9px] text-[#555] font-bold uppercase border-b border-[#222] pb-1 mb-2">Tasks</div>
            <div className="space-y-4 overflow-y-auto pr-1">
              <div>
                <div className="flex justify-between text-[9px] text-[#888] mb-1 font-bold"><span>SPRINT</span><span>66%</span></div>
                <div className="w-full bg-[#222] h-1 border border-[#333] overflow-hidden">
                  <div className="bg-[#4ec9b0] h-full" style={{ width: '66%' }}></div>
                </div>
              </div>
              <div className="space-y-1.5 text-[10px]">
                <div className="flex items-start gap-2 text-[#4ec9b0] italic font-bold"><span>[X]</span><span>CLI Logic</span></div>
                <div className="flex items-start gap-2 text-[#4ec9b0] italic font-bold"><span>[X]</span><span>Add Worker</span></div>
                <div className="flex items-start gap-2 text-[#dcdcaa]"><span>[{'>'}]</span><span>Multiply Logic</span></div>
                <div className="flex items-start gap-2 text-[#444]"><span>[ ]</span><span>Result Format</span></div>
              </div>
            </div>
          </div>
        </div>

        {/* MAIN WORKSPACE: SWAPPABLE VIEWS */}
        <div className="flex-1 border border-[#333] bg-[#050505] flex flex-col relative overflow-hidden">
          <div className="bg-[#161b22] px-2 py-1.5 border-b border-[#333] flex justify-between items-center shrink-0">
            <div className="flex gap-2">
              {['DIAGRAM', 'FILES', 'TIMELINE', 'MISSION'].map((tab, idx) => (
                <button
                  key={tab}
                  onClick={() => setMainTab(tab)}
                  className={`text-[9px] font-bold px-2 py-0.5 rounded border ${mainTab === tab ? 'bg-[#1a2b3c] text-[#569cd6] border-[#264f78]' : 'text-[#555] border-transparent'}`}
                >
                  {idx + 1}:{tab}
                </button>
              ))}
            </div>
            <span className="text-[8px] text-[#333] font-bold uppercase">Main Workspace</span>
          </div>

          <div className="flex-1 overflow-auto p-4 relative">
            {mainTab === 'DIAGRAM' && (
              <pre className="whitespace-pre leading-[1.2] scale-[0.9] origin-top-left">
                {`  ┌──────────────────────────────────────────────────────────┐\n`}
                {`  │  [ARCHITECTURE FLOW]                                     │\n`}
                {`  └────────────────────────────┬─────────────────────────────┘\n`}
                {`                               │\n`}
                {`                `}
                <ColorSpan color={colors.magenta}>{`┌──────────────┴──────────────┐`}</ColorSpan>{`\n`}
                {`                `}
                <ColorSpan color={colors.magenta}>{`│  `}</ColorSpan>
                <ColorSpan color={colors.green}>{S}</ColorSpan>
                <ColorSpan color={colors.magenta}>{` CLI_PARSER               │`}</ColorSpan>{`\n`}
                {`                `}
                <ColorSpan color={colors.magenta}>{`└──────────────┬──────────────┘`}</ColorSpan>{`\n`}
                {`                               │\n`}
                {`                `}
                <ColorSpan color={colors.blue}>{`┌──────────────┴──────────────┐`}</ColorSpan>{`\n`}
                {`                `}
                <ColorSpan color={colors.blue}>{`│  `}</ColorSpan>
                <ColorSpan color={colors.yellow}>{S}</ColorSpan>
                <ColorSpan color={colors.blue}>{` ARITHMETIC_ENG          │`}</ColorSpan>{`\n`}
                {`                `}
                <ColorSpan color={colors.blue}>{`└──────────┬───┬───┬──────────┘`}</ColorSpan>{`\n`}
                {`                           │   │   │\n`}
                {`           ┌───────────────┘   │   └───────────────┐\n`}
                {`           ▼                   ▼                   ▼\n`}
                <ColorSpan color={colors.cyan}>{`   ┌───────────────┐   ┌───────────────┐   ┌───────────────┐`}</ColorSpan>{`\n`}
                <ColorSpan color={colors.cyan}>{`   │ `}</ColorSpan>
                <ColorSpan color={colors.green}>{S}</ColorSpan>
                <ColorSpan color={colors.cyan}>{` ADD_METHOD  │   │ `}</ColorSpan>
                <ColorSpan color={colors.green}>{S}</ColorSpan>
                <ColorSpan color={colors.cyan}>{` SUB_METHOD  │   │ `}</ColorSpan>
                <ColorSpan color={colors.red}>{S}</ColorSpan>
                <ColorSpan color={colors.cyan}>{` MUL_METHOD  │`}</ColorSpan>{`\n`}
                <ColorSpan color={colors.cyan}>{`   └───────┬───────┘   └───────┬───────┘   └───────┬───────┘`}</ColorSpan>{`\n`}
                {`           │                   │                   │\n`}
                {`           └───────────────┐   │   ┌───────────────┘\n`}
                {`                           ▼   ▼   ▼\n`}
                <ColorSpan color={colors.magenta}>{`                ┌──────────────┴──────────────┐`}</ColorSpan>{`\n`}
                <ColorSpan color={colors.magenta}>{`                │  `}</ColorSpan>
                <ColorSpan color={colors.gray}>{S}</ColorSpan>
                <ColorSpan color={colors.magenta}>{` FORMATTER                │`}</ColorSpan>{`\n`}
                <ColorSpan color={colors.magenta}>{`                └─────────────────────────────┘`}</ColorSpan>
              </pre>
            )}

            {mainTab === 'FILES' && (
              <div className="space-y-2">
                <div className="text-[10px] text-[#555] border-b border-[#222] pb-1 font-bold uppercase">Source Tree</div>
                <pre className="text-[11px] leading-relaxed">
                  <ColorSpan color={colors.text}>{`root/\n`}</ColorSpan>
                  <ColorSpan color={colors.magenta}>{`├── ${S} cli/\n`}</ColorSpan>
                  <ColorSpan color={colors.text}>{`│   └── parser.py ......................... `}</ColorSpan><ColorSpan color={colors.green}>{S}</ColorSpan>{`\n`}
                  <ColorSpan color={colors.blue}>{`├── ${S} engine/\n`}</ColorSpan>
                  <ColorSpan color={colors.text}>{`│   ├── calculator.py ..................... `}</ColorSpan><ColorSpan color={colors.yellow}>{S}</ColorSpan>{`\n`}
                  <ColorSpan color={colors.text}>{`│   │   ├── `}</ColorSpan><ColorSpan color={colors.green}>{S}</ColorSpan><ColorSpan color={colors.cyan}>{` add()\n`}</ColorSpan>
                  <ColorSpan color={colors.text}>{`│   │   ├── `}</ColorSpan><ColorSpan color={colors.green}>{S}</ColorSpan><ColorSpan color={colors.cyan}>{` sub()\n`}</ColorSpan>
                  <ColorSpan color={colors.text}>{`│   │   └── `}</ColorSpan><ColorSpan color={colors.red}>{S}</ColorSpan><ColorSpan color={colors.cyan}>{` mul()\n`}</ColorSpan>
                  <ColorSpan color={colors.text}>{`│   └── types.py .......................... `}</ColorSpan><ColorSpan color={colors.green}>{S}</ColorSpan>{`\n`}
                  <ColorSpan color={colors.magenta}>{`└── ${S} output/\n`}</ColorSpan>
                  <ColorSpan color={colors.text}>{`    └── formatter.py ...................... `}</ColorSpan><ColorSpan color={colors.gray}>{S}</ColorSpan>
                </pre>
              </div>
            )}

            {mainTab === 'TIMELINE' && (
              <div className="space-y-4">
                <div className="text-[10px] text-[#555] border-b border-[#222] pb-1 font-bold uppercase">Project Changes Timeline</div>
                <div className="space-y-0 pl-2">
                  {[
                    { time: '11:12 AM', side: 'DESIGN', msg: 'Updated multiplication rules to exclude side-effects', color: colors.blue },
                    { time: '10:45 AM', timeLabel: '1h ago', side: 'CODE', msg: 'Added file logging to MUL_METHOD for debugging', color: colors.green },
                    { time: '10:00 AM', timeLabel: '2h ago', side: 'CODE', msg: 'Initial implementation of ADD and SUB workers', color: colors.green },
                    { time: '09:30 AM', timeLabel: 'Morning', side: 'DESIGN', msg: 'Finalized core arithmetic engine interface', color: colors.blue }
                  ].map((item, idx) => (
                    <div key={idx} className="flex gap-4 group">
                      <div className="w-20 text-[#555] text-[9px] shrink-0">{item.time}</div>
                      <div className="relative border-l border-[#333] pl-4 pb-6 group-last:border-transparent">
                        <div className="absolute -left-[5px] top-1 w-2 h-2 rounded-full border border-black" style={{ backgroundColor: item.color }}></div>
                        <div className="font-bold text-[10px]" style={{ color: item.color }}>{item.side}</div>
                        <div className="text-[#888]">{item.msg}</div>
                      </div>
                    </div>
                  ))}
                </div>
              </div>
            )}

            {mainTab === 'MISSION' && (
              <div className="space-y-6">
                <div className="text-[10px] text-[#555] border-b border-[#222] pb-1 font-bold uppercase">Mission Control: Objectives</div>
                <div className="grid grid-cols-1 gap-4">
                  {[
                    { id: 'M1', name: 'High Precision Engine', status: 'In Progress', body: 'The core engine must support calculations with precision up to 20 decimal places without rounding errors.' },
                    { id: 'M2', name: 'Zero Dependency CLI', status: 'Done', body: 'The binary must compile down to a single executable that runs on clean Linux/macOS environments without pre-installed libs.' },
                    { id: 'M3', name: 'Bitwise Performance', status: 'Deviation', body: 'Replace standard math operators with manual bit-level manipulation to achieve maximum execution speed.' }
                  ].map(mission => (
                    <div key={mission.id} className="border border-[#222] bg-[#0c0c0c] p-3 rounded shadow-lg">
                      <div className="flex justify-between items-center mb-2">
                        <span className="font-bold text-[#569cd6]">{mission.id}: {mission.name}</span>
                        <span className={`text-[8px] font-bold px-1 rounded uppercase border ${
                          mission.status === 'Done' ? 'text-green-500 border-green-900 bg-green-950/20' :
                          mission.status === 'In Progress' ? 'text-amber-500 border-amber-900 bg-amber-950/20' :
                          'text-red-500 border-red-900 bg-red-950/20'
                        }`}>{mission.status}</span>
                      </div>
                      <p className="text-[#888] leading-relaxed border-l-2 border-[#333] pl-3 italic">{mission.body}</p>
                    </div>
                  ))}
                </div>
              </div>
            )}
          </div>
        </div>

        {/* RIGHT SIDEBAR: INFORMATION HUB */}
        <div className="w-[420px] border border-[#333] bg-[#0c0c0c] flex flex-col shadow-2xl overflow-hidden shrink-0">
          <div className="bg-[#1a1a1a] p-2 font-bold text-[#888] flex justify-between border-b border-[#333] shrink-0">
            <span className="text-[10px] uppercase">{selected.name} Inspection</span>
            <div className="flex gap-4 text-[9px]">
               <span className="text-blue-400 cursor-pointer hover:underline" onClick={() => {
                 const keys = Object.keys(projectData);
                 const next = keys[(keys.indexOf(selectedId) + 1) % keys.length];
                 setSelectedId(next);
               }}>[TAB] NEXT</span>
               {selected.isDeviating && (
                 <span className="text-red-400 cursor-pointer animate-pulse font-bold" onClick={() => setInfoView(infoView === 'DESIGN' ? 'DIFFERENCES' : 'DESIGN')}>
                    [D] {infoView === 'DIFFERENCES' ? 'BACK' : 'DIFF'}
                 </span>
               )}
            </div>
          </div>

          <div className="p-4 flex-1 overflow-y-auto scrollbar-hide">
            {infoView === 'DESIGN' ? (
              <div className="space-y-6">
                <section>
                  <div className="text-[9px] text-[#569cd6] font-bold uppercase mb-1 tracking-tighter">Mission / Description</div>
                  <div className="text-[11px] font-bold leading-tight text-white/90">{selected.description}</div>
                </section>

                <section className="bg-[#111] border border-[#222] p-2 space-y-2">
                  <div>
                    <div className="text-[8px] text-[#555] font-bold uppercase mb-1">Interface Planning</div>
                    <div className="text-[#4ec9b0] italic font-bold">{selected.planning.interface}</div>
                  </div>
                  <div className="flex justify-between items-center text-[9px] border-t border-[#222] pt-1">
                    <span className="text-[#555]">Logical Style:</span>
                    <span className="text-[#569cd6] font-bold underline">{selected.planning.style}</span>
                  </div>
                </section>

                <section className="bg-[#4ec9b0]/5 border-l-2 border-[#4ec9b0] p-2">
                  <div className="text-[9px] text-[#4ec9b0] font-bold mb-1 uppercase">Actual Code Snapshot</div>
                  <div className="space-y-1 text-[10px]">
                    <div className="flex justify-between"><span>Dependencies:</span><span className="text-gray-300">{selected.actual.deps.join(', ')}</span></div>
                    <div className="flex justify-between"><span>Side Effects:</span><ColorSpan color={selected.actual.sideEffects === 'None' ? colors.text : colors.yellow}>{selected.actual.sideEffects}</ColorSpan></div>
                    <div className="flex justify-between"><span>Complexity:</span><span className="text-gray-400">{selected.actual.complexity}</span></div>
                  </div>
                </section>

                <section>
                  <div className="text-[9px] text-[#555] font-bold mb-2 uppercase border-b border-[#222] pb-1">Project Rules</div>
                  <ul className="space-y-2">
                    {selected.planning.rules.map((rule, i) => (
                      <li key={i} className="flex gap-2 items-start">
                        <span className="text-[#569cd6] mt-1 font-bold">»</span>
                        <span className="text-[#888] text-[10px] leading-snug">{rule}</span>
                      </li>
                    ))}
                  </ul>
                </section>

                {selected.isDeviating && (
                  <div className="mt-8 border border-red-900 bg-red-950/20 p-2 text-[10px] text-red-500 flex items-center justify-between">
                    <span className="font-bold">⚠ CODE DEVIATED FROM PLAN</span>
                    <span className="underline cursor-pointer font-bold" onClick={() => setInfoView('DIFFERENCES')}>ANALYZE [D]</span>
                  </div>
                )}
              </div>
            ) : (
              <div className="space-y-4 animate-in fade-in duration-300">
                <div className="bg-red-900/10 border border-red-900/30 p-2 text-red-500 font-bold text-[10px] uppercase text-center tracking-widest">Mismatch Comparison</div>

                <div className="grid grid-cols-2 gap-2 text-[10px] bg-black/40 p-2 border border-[#222]">
                  <div className="text-[#555] uppercase font-bold text-[8px] pb-1 border-b border-[#222]">Design Plan</div>
                  <div className="text-[#555] uppercase font-bold text-[8px] pb-1 border-b border-[#222]">Actual Code</div>

                  <div className="text-white py-1">Interface: {selected.planning.interface.split('->')[0]}</div>
                  <div className="text-red-400 py-1 font-bold italic">{selected.actual.interface}</div>

                  <div className="text-white py-1 border-t border-[#222]">No Side Effects</div>
                  <div className="text-red-400 py-1 font-bold border-t border-[#222]">{selected.actual.sideEffects}</div>

                  <div className="text-white py-1 border-t border-[#222]">No Deps</div>
                  <div className="text-red-400 py-1 border-t border-[#222]">{selected.actual.deps.join(', ')}</div>
                </div>

                <div className="p-3 bg-[#050505] border border-red-900/30 text-[10px] text-[#888] leading-relaxed">
                  <span className="text-red-500 font-bold uppercase">[Diagnostics]:</span> The implementation of <span className="text-white font-bold">{selected.name}</span> has drifted significantly. It introduces file system access and loose typing ('any') which violates the strict mathematical intent defined in the blueprint.
                </div>
                <button
                  className="w-full py-2 bg-[#1a1a1a] text-[#888] border border-[#333] text-[9px] uppercase font-bold hover:text-white"
                  onClick={() => setInfoView('DESIGN')}
                >
                  Return to Planning
                </button>
              </div>
            )}
          </div>

          <div className="p-2 border-t border-[#333] bg-[#050505] text-[9px] text-[#555] flex justify-between italic">
            <span>[E] Edit Design</span>
            <span>[S] Re-Sync Data</span>
          </div>
        </div>
      </div>

      {/* FOOTER HOTKEYS */}
      <div className="mt-2 text-[9px] flex gap-5 text-[#555] border-t border-[#333] pt-2 shrink-0">
        <div className="flex gap-4">
          <span><span className="text-[#888] font-bold px-1 bg-[#222]">[1]</span> MAP</span>
          <span><span className="text-[#888] font-bold px-1 bg-[#222]">[2]</span> FILES</span>
          <span><span className="text-[#888] font-bold px-1 bg-[#222]">[3]</span> HISTORY</span>
          <span><span className="text-[#888] font-bold px-1 bg-[#222]">[4]</span> MISSION</span>
          <span><span className="text-[#888] font-bold px-1 bg-[#222]">[TAB]</span> CYCLE</span>
          <span className="ml-4 opacity-50 border-l border-[#333] pl-4 uppercase font-bold">Ctrl+C Quit</span>
        </div>
        <div className="ml-auto flex gap-4 uppercase font-bold opacity-30 tracking-tighter italic">
          <span>PID: 9921</span>
          <span>Vibe_Daemon v1.6.0</span>
        </div>
      </div>
    </div>
  );
};

export default App;
